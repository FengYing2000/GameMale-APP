name: gm_server
description: GameMale PWA 後端 —— 轉發論壇、輪詢提醒、送 Web Push
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  pointycastle: ^4.0.0
  http: ^1.2.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
